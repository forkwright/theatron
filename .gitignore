/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
*.swp
target/
*.bak

# kanon substrate database — created by `kanon gate` / `kanon lint` runs
# (fjall on-disk KV cache). Should never be committed; the journal alone
# is 33 MB and per-machine state has no value in version control.
.kanon/
