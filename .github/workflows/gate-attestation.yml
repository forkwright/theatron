name: Gate Attestation

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  gate:
    uses: forkwright/.github/.github/workflows/gate-attestation.yml@961f4e0d8ef84ac93303e9fb1ea4123c98b5dab1
