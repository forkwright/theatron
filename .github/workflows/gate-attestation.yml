name: Gate Attestation

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  gate:
    uses: forkwright/.github/.github/workflows/gate-attestation.yml@66867bca65b34a464f184cf1d6d5c9ee433c4dba
