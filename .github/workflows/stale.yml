name: Stale

on:
  schedule:
    - cron: "0 12 * * 0"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  stale:
    uses: forkwright/.github/.github/workflows/stale.yml@bdbc9617a20369d1dca732344032027119808253
