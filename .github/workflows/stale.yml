name: Stale

on:
  schedule:
    - cron: "0 12 * * 0"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: forkwright/.github/.github/workflows/stale.yml@main
